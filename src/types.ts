export interface Skill {
  step: number;
  tree: string;
  treeLabel: string;
  treeIcon: string;
  treeColor: string;
  treeGlow: string;
  treeBg: string;
  name: string;
  tag: string;
  why: string;
}
